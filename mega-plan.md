# Mega Plan: Make the Hardware Deal Tracker actually work end-to-end (poll -> score -> notify -> dashboard) and extend it with price history, AI deal analysis, observability, multi-source ingestion, and a user-managed catalog.
> Features: 6 | Batches: 3 | Mode: auto
## Features
### feature-001: Make Data Flow (scheduler + poller wiring + scoring-in-poll) [HIGH]
KEYSTONE feature (MVP2 Phase 01). Make the ingest pipeline real and scheduled. Tasks: (T1.3) Add `apscheduler>=3.11.2,<4` to backend/pyproject.toml and add a FastAPI `lifespan` context manager in app/main.py that starts an AsyncIOScheduler with an IntervalTrigger calling a `_poll_tick()` wrapper (opens its own AsyncSessionLocal/session_factory, runs EbayPoller().search_all(db)); flags coalesce=True, max_instances=1, replace_existing=True, shutdown(wait=False). (config) Add SCHEDULER_ENABLED: bool = True and POLL_SCHEDULER_INTERVAL: int = 300 to app/core/config.py; migrate `class Config` to `model_config = SettingsConfigDict(...)` to kill Pydantic v2 deprecation warnings (this feature owns the config.py modernization; other features add their vars on top). (T1.2) Replace the STUB returns in app/api/v1/endpoints/search.py: POST /search/trigger/{item_id} resolves the TrackedItem via db.get then calls EbayPoller().search_item(db, item); POST /search/trigger-all calls search_all(db); honor RateBudgetManager (return 429 + Retry-After when budget exhausted); keep Depends(get_db)+Depends(get_current_user). (GAP beyond written plan) The poller currently does NOT call the scoring engine -- wire DealScoringEngine.calculate_overall_score(listing, historical_stats, catalog_item) into the poll path (search_item) so ingested listings are scored and persisted with their score; this is required for listings to render *scored* in the dashboard (Phase 01 exit) and for the notification dispatcher (feature-004) to have scores to gate on. (T1.1) The eBay client filter f-string + OAuth scope bugs are ALREADY FIXED in client.py; this feature only ADDS the unit test asserting the built filter string matches the canonical fixtures (buyingOptions:{FIXED_PRICE|AUCTION}, conditionIds:{1000|3000}, price:[50..200],priceCurrency:USD, comma-joined). (T1.4) Add a `@pytest.mark.real_ebay` smoke test (skipped unless creds set) that searches EPYC 7F72 and asserts >=1 listing persisted. Exit: docker compose up -> scheduler logs a tick each interval, mock+real listings accumulate scored, dashboard renders them.
---
### feature-002: Quality, CI & Ops Hygiene (tests + GitHub Actions + image trim + n8n removal + observability) [HIGH]
MVP2 Phase 01 tests + Phase 03 infra + NEW observability. Tasks: (T1.5) test scaffold -- backend/tests/conftest.py with async test client fixture, isolated test DB, SCHEDULER_ENABLED=False, mock eBay client always on; backend/tests/__init__.py; register `real_ebay` marker in [tool.pytest.ini_options].markers. (T1.6) smoke suite: test_health, test_auth (register+login->JWT->GET /items), test_scoring (DealScoringEngine known inputs), test_ebay_client (filter fixture), test_poller (mock client inserts >=1 scored listing per enabled item). (T3.6) test_rate_budget (200 buffer, 4000 near-limit priority skip, 5000 hard stop) and test_catalog (34 items, scam_floor present, generate_seed parity -- coordinate with feature-005). (T3.7) Multistage backend/Dockerfile: the runtime stage currently copies dev deps from builder -- fix so runtime installs PRODUCTION deps only (drop pytest/ruff/pytest-asyncio/pytest-cov), target <300MB; builder stage keeps [dev] for CI. (T3.8) Makefile `up` target runs `docker compose build` first so NEXT_PUBLIC_* changes propagate; README documents port-8001 remap + build requirement. (T3.1) Remove n8n: delete the n8n service block + n8n_data volume from docker-compose.yml, drop N8N_* from .env.example, update README tech-stack/features; document the docker-host-01 teardown commands. (T3.3) server_default audit: add server_default to TrackedItem.marketplace/alert_threshold/min_deal_score/is_enabled/search_interval and User.is_active/is_admin; generate+apply an Alembic migration. (T3.5) idempotent seed: add ON CONFLICT (name) DO NOTHING/UPDATE to tracked_items inserts in seed_data_v2.sql. (T3.4) items.py list_items: add response_model with priority_tier as a Pydantic v2 @computed_field (priority_tier currently serialized via a manual dict helper). (T3.9) NEW .github/workflows/ci.yml: astral-sh/setup-uv, uv sync, postgres+redis service containers, run `make test`, optional frontend `npm ci && npm run build`. (NEW observability) Add a Prometheus metrics exporter (prometheus-client or prometheus-fastapi-instrumentator) exposing /metrics: counters/histograms for poll cycles, listings ingested, scoring runs, rate-budget burn (calls_today/remaining/utilization), eBay API errors/429s, scheduler tick duration; commit a Grafana dashboard JSON under ops/grafana/. Tests + metrics depend on feature-001's wired poll/score path.
**Dependencies:** feature-001
---
### feature-003: Notifications Dispatcher + Digest + Auth Lockdown [HIGH]
MVP2 Phase 02. Notification services are ALREADY implemented (telegram.py send_deal_alert; email.py send_email + send_deal_alert). Remaining: (T2.0) add NOTIFICATIONS_ENABLED: bool = True and SMTP_FROM: str = '' to config.py (on top of feature-001's config modernization); update .env.example. (T2.3) NEW app/services/notifications/dispatcher.py with NotificationDispatcher.dispatch_for_deal(db, listing, score): iterate NotificationSetting rows; skip if mute_until > now; if telegram_enabled and score >= telegram_min_score and telegram_chat_id -> send_deal_alert(...); if email_enabled and score >= email_min_score and email_address -> if email_digest_mode == 'instant' send_deal_alert else defer to digest. Wrap each channel in try/except+log so one failure doesn't sink the other. Hook the dispatcher into the poll/score path established by feature-001 (call after scoring). NOTE: model uses per-channel telegram_min_score(70)/email_min_score(50), NOT a single alert_threshold. (NEW digest) Implement EmailClient.send_deal_digest(to, deals) + a digest job (daily/weekly per email_digest_mode) that batches deals scored above email_min_score since the last digest; wire via the scheduler (coordinate interval/job-id with feature-001). NOTE current email.py uses BLOCKING smtplib in async methods -- migrate to aiosmtplib (or run_in_executor) so digest/alert sends don't block the event loop. (tests) dispatcher integration tests (above thresholds -> both channels called; below -> neither), telegram/email unit tests mocking transport. (T2.5) close /auth/register behind ALLOW_REGISTRATION: bool = False (403 when off); make PUT /settings/notifications upsert (currently 404s on missing row; GET already lazy-creates). (T2.6) fail-loud SECRET_KEY: raise RuntimeError on boot if SECRET_KEY == 'change-me-in-production-min-32-chars' or unset; remove the placeholder default from docker-compose.yml; document `openssl rand -hex 32` in .env.example. (T2.7) frontend error toasts: add `sonner`, wrap optimistic toggle/delete/updateInterval in app/items/page.tsx with try/catch -> revert + toast. (T2.4) manual end-to-end smoke with real Telegram+SMTP creds, record message IDs in journal.
**Dependencies:** feature-001
---
### feature-004: Catalog Source-of-Truth Refactor + User-Managed Catalog CRUD (fresh-DB capable) [MEDIUM]
MVP2 Phase 03 D1/D2 + NEW user-managed catalog. (T3.2 source-of-truth) Drop the BENCHMARK_PRICES dict from app/services/scoring/engine.py (22 entries that drift from catalog medians) and always read the benchmark from the TrackedItem/catalog_item passed in (engine already accepts catalog_item=None -- make callers resolve+pass the TrackedItem; coordinate the engine.py edits with feature-001's scoring-in-poll wiring to minimize merge conflicts). Add scripts/generate_seed.py that reads the Python HardwareCatalog (34 items) and writes seed_data_v2.sql deterministically; wire `make seed-regen`; keep generated SQL committed but treat catalog.py as the source of truth; add the catalog<->seed parity test (coordinate with feature-002 test_catalog). (NEW user-managed catalog CRUD) Backend: full CRUD endpoints for tracked items (create/read/update/delete: name, search terms, category, benchmark_median, scam_floor, search_interval, alert thresholds, is_enabled) admin-gated. Frontend: admin UI under app/items (or app/admin) to add/edit/delete tracked items, including editing the previously-curated items. HARD REQUIREMENT (per Sean): the entire app MUST operate correctly starting from an EMPTY catalog/fresh database -- the 34 seeded items become OPTIONAL starter data, never a hard dependency (no code path may assume they exist). Add a test that boots with zero tracked items and verifies dashboard/poller/endpoints degrade gracefully, then a user-created item flows through poll->score->dashboard.
**Dependencies:** feature-001
---
### feature-005: Multi-Source Ingestion Abstraction (eBay + researched US sources + Micro Center) [MEDIUM]
NEW. Generalize ingestion beyond eBay. Refactor the eBay-specific poll/client into a SourceAdapter abstraction (common interface: search(catalog_item) -> list[NormalizedListing] with fields source, source_listing_id, title, url, price, currency, shipping, condition, availability, seller/merchant, catalog_item_id, raw_payload, fetched_at). Refactor the existing EbayBrowseClient/EbayPoller into the first adapter WITHOUT regressing feature-001's scheduler/scoring wiring (depends on feature-001 merged). SOURCE #2 (priority, per Sean): build our OWN PcPartPickerAdapter -- but research (plan/MVP2_PCPARTPICKER_RESEARCH.md) concluded it is a price-BENCHMARK/REFERENCE source ONLY, NOT a deal-listing feed: refresh catalog benchmark_median + add a 'vs retail' delta; do NOT route PCPartPicker rows through the eBay scoring/dedup/notification pipeline (condition is always 'new'). Overlap is only ~10-12 of 34 catalog items (workstation GPUs, consumer NVMe/SSD, non-ECC DDR, maybe Threadripper); USELESS for EPYC / RDIMM-LRDIMM ECC / enterprise HDD / server boards -- scope the adapter to mappable items via an optional manually-resolved pcpp_product_id on those TrackedItems. Implementation: REUSE the data model from github.com/lucwl/pypartpicker (Part/Vendor/Price/specs; has a response_retriever hook) but REPLACE its stale requests-html/pyppeteer transport (Cloudflare flags it). github.com/docyx/pc-part-dataset = one-time spec bootstrap only; github.com/N-O-U-R = paid-ZenRows, low reuse. Anti-bot/cost strategy CHEAPEST-FIRST (target $0/mo; volume is tiny ~10-12 pages/day): (a) curl_cffi TLS-impersonation + polite rate limits + response caching, (b) egress via a RESIDENTIAL Tailscale exit node from Sean's homelab -- NOT the datacenter IP 104.223.27.177 (Cloudflare flags datacenter ranges), (c) Nodriver/Camoufox/FlareSolverr ONLY to mint Cloudflare clearance cookies if blocked, (d) paid scraping APIs (Zyte ~$0.13-16/1k, Bright Data ~$0.75/1k) behind a feature flag as last resort. Own rate bucket <=200/day (separate from eBay's 5000), slow-tier scheduling, circuit-breaker on Cloudflare errors, ENABLE_PCPARTPICKER=false by default, fully env-configurable. HEAVY ToS CAVEAT: PCPartPicker's ToS explicitly forbids scraping/price collection and there is NO public JSON API (staff refuse) -- keep volume minimal, cache aggressively, never redistribute, keep it disableable. SOURCE #3+ (follow-on, lower priority): a generic ShopifyJsonLdAdapter for US used-server retailers that expose public /products.json + schema.org JSON-LD -- onboard TechMikeNY > UnixSurplus > ServerMonkey (see plan/MVP2_SOURCE_RESEARCH.md). DEFER Amazon PA-API (Associates approval + 10 sales/30d + bans static price caching) and Micro Center (anti-bot, consumer-focused) to MVP3; leave clean seams. CAVEAT: confirm platform + robots.txt/ToS per site before coding each adapter. Make dedup/scoring/notifications/rate-budget source-aware: dedup on (source, source_listing_id) (TechMikeNY appears in both its site and the eBay feed); give RateBudgetManager separate per-source budget buckets (polite self-imposed limits, NOT eBay's 5000/day). Document how to add a new adapter. Tests: adapter-interface conformance test + a mock-driven PCPartPicker parse/poll test + a Shopify-products.json poll test + a cross-source dedup test.
**Dependencies:** feature-001
---
### feature-006: Price History & Trends + AI Deal Analysis (configurable provider) [MEDIUM]
NEW. Two intelligence capabilities over ingested+scored listings. (Price history) New PriceHistory model + Alembic migration capturing per-item (and/or per-listing) price points over time (min/median/sample price per poll snapshot); record a point on each poll tick; API endpoint(s) to fetch a time series; dashboard trend charts (recharts is already a frontend dep) showing price-vs-time and 'vs 30/90d median' deltas so a user can tell whether a current price is actually good historically. (AI deal analysis) New app/services/ai/ analysis service with a CONFIGURABLE provider: default OpenRouter (Gemini 2.5 Flash primary, Qwen 3.6 Plus / GPT-4o-mini fallback -- OPENROUTER_API_KEY/OPENROUTER_MODEL already in config) with an opt-in local vLLM endpoint on the DGX Spark selectable via env (e.g. AI_PROVIDER=openrouter|vllm, AI_VLLM_BASE_URL). Three analyses per listing (NO photo/vision): (1) natural-language deal grade + short reasoning ('strong buy: 22% under 90d median, reputable seller'); (2) scam/fraud signal from title/description text (vague specs, too-cheap, drop-ship language) raising a flag beyond the numeric scam_floor; (3) spec/compatibility extraction parsing listing text into structured specs (cores, TDP, socket, capacity) to improve catalog matching. Persist AI results (store provider+model+verdict+flags+extracted specs, with cost/latency guards and graceful degradation if the provider is down) and surface them in the dashboard listing/detail views. Depends on feature-001 (scored listings exist) and feature-004 (catalog/specs for compatibility + benchmark from catalog). Tests mock the LLM client; assert prompt payload + parsing of a canned response; assert the app works with AI disabled.
**Dependencies:** feature-001, feature-004
---
## Execution Batches
- Batch 1: feature-001 (merge to target branch)
- Batch 2 (Parallel): feature-002, feature-003, feature-004, feature-005 (merge to target branch)
- Batch 3: feature-006
