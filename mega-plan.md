# Mega Plan: Harden and surface the Hardware Deal Tracker: expose every MVP2 backend capability in the established dark-terminal UI, move auth server-side, deepen scoring with real eBay sold-comps, bring deferred data sources online, and clear the quality/ops backlog.
> Features: 7 | Batches: 2 | Mode: auto
## Features
### feature-001: eBay Sold-Comps Historical Baseline (fill the scoring seam) [HIGH]
Deepen deal scoring with a real market-history baseline instead of only the static catalog benchmark_median. Fill the `EbayPoller._historical_stats_for(item)` seam (currently returns {}) with stats computed from eBay Browse SOLD/COMPLETED comps for the item over a rolling 90-day window: Tukey-trimmed median + IQR + min/avg/std_dev + data_points, matching the dict shape DealScoringEngine.calculate_overall_score already consumes (median_price, avg_price, std_dev, min_price, data_points). Reimplement the statistical approach from the bullseye-app pattern (do NOT copy AGPL code). Add a sold-comps fetch path (Browse API supports filtering; if sold/completed is not available via Browse for the app token, fall back to accumulating our own observed PriceHistory as the rolling baseline and document the limitation). Persist a per-item rolling stats snapshot (new model + migration) refreshed on a cadence, with a configurable lookback window and trim factor; gracefully degrade to the catalog benchmark when insufficient comps. Expose the resolved baseline (median, IQR band, vs-median) so feature-005 can render it and the AI panel can cite it. Also emit a TREND DIRECTION signal (rising/falling/stable) from the rolling window slope — the category research repeatedly flags actionable trends (e.g. 'DDR4 ECC RISING, buy sooner'); persist it so feature-005 can surface a trend chip and the AI panel/alerts can act on it. NOTE the eBay constraint: sold/completed comps may not be exposed to the Browse app token — if so, fall back to accumulating our own PriceHistory as the rolling baseline and document the limitation (see plan/MVP3_DEFERRED_BACKLOG.md B). TDD: stats math unit tests (trimming/IQR on fixtures), trend-direction test, fallback-to-benchmark test, and a poller test asserting scores use the new baseline when present. Coordinate the engine/_historical_stats_for edit to minimize conflict with the existing poll path.
---
### feature-002: Server-Side Auth: httpOnly Cookie + Next Middleware + CORS [HIGH]
Close the MVP1/MVP2 auth gaps (DEFERRED_ISSUES S1/S2/S4). Move authentication off the client-side localStorage guard: (a) backend issues the JWT in an httpOnly, Secure, SameSite cookie via a login route (keep the bearer flow working for API clients/tests, but the browser session uses the cookie); add a logout route that clears it. (b) Frontend: add Next.js `middleware.ts` that reads the session cookie and protects routes server-side (no flash-of-dashboard-before-redirect that the client AuthGuard caused); update lib/api.ts to rely on the cookie (credentials: 'include') and stop reading token from localStorage; keep the login page writing the cookie via the backend. (c) CORS: make `allow_origins` a configurable list (lab subdomain *.lab.lsdmt.me + localhost) instead of a single FRONTEND_URL, with credentials allowed. Preserve get_current_user for both cookie and bearer. TDD: backend cookie set/clear + protected-route-with-cookie tests; document the middleware behavior. This feature OWNS the auth-related frontend files (auth-guard/middleware/login/api auth) so feature-005 rebases on it.
---
### feature-003: Multi-Source Go-Live: Shopify adapters live + PCPartPicker eval [MEDIUM]
Bring the built-but-dormant SourceAdapters into production. (a) Activate the generic ShopifyJsonLdAdapter for TechMikeNY -> UnixSurplus -> ServerMonkey: verify each site's platform + robots.txt/ToS before enabling, wire per-source config + per-source rate buckets (separate from eBay's 5000/day), ensure cross-source dedup on (source, source_listing_id), and route Shopify listings through the normal score/persist path (they ARE deal listings, unlike PCPartPicker). Add a real (mock-transport) integration test per onboarded site against a captured /products.json fixture. (b) PCPartPicker: keep ENABLE_PCPARTPICKER=false default but make it genuinely runnable behind a residential Tailscale egress flag (benchmark-only, never scored); document the egress setup and circuit-breaker. (c) Evaluate deferred Amazon PA-API (Associates quota + no price caching) and Micro Center (anti-bot) and record a go/no-go for MVP4 in mega-findings. After the three primaries, onboard the next-easiest Shopify-class retailers from the research backlog: Cloud Ninjas (easy Shopify), Natex (high deal-density cheap DDR4/NICs/barebones), and SaveMyServer (weekly-refreshed pricing → low-cadence MEMORY price signal during the DDR4 shortage) — same ShopifyJsonLdAdapter, per-source config + rate bucket; verify robots/ToS each. (d) Populate the optional pcpp_product_id mapping for the ~10-12 PCPartPicker-mappable catalog items (workstation GPU, consumer NVMe/SSD, non-ECC DDR, Threadripper) so the benchmark adapter has targets; do NOT auto-search PCPartPicker by our eBay-tuned keywords. (e) Document the eBay 5,000/day ceiling + the Application Growth Check path for higher caps (out of scope to request now). Record an explicit go/no-go for Amazon PA-API (Associates gate) and Micro Center for MVP4 in mega-findings. Surface a per-listing source so feature-005 can badge it. TDD: Shopify products.json parse/poll tests (one per onboarded site against a captured fixture), cross-source dedup test, rate-bucket isolation test.
---
### feature-004: Quality & Ops Hardening: ruff backlog, bcrypt, categories, docs, live smoke [MEDIUM]
Clear the residual backlog so CI is strict and the stack is production-clean. (a) Fix the ~143 legacy ruff violations across untouched files (typing.Optional -> X|None, == True/== None -> .is_(), unused imports, etc.) and FLIP the CI ruff lint step from continue-on-error to BLOCKING. (b) Migrate off unmaintained passlib to direct bcrypt or argon2-cffi for password hashing (DEFERRED_ISSUES I9), keeping existing hashes verifiable or providing a rehash-on-login path; drop the bcrypt<4 pin. (c) Split eBay HDD vs SSD category ids (D3) so enterprise-HDD searches aren't polluted by SSDs; verify the category ids and update the catalog. (d) Refresh stale docs: AGENTS.md / .agents/*.md paths now under project/ (DOC2), remove n8n references (DOC3, n8n was deleted in MVP2), README ports/run instructions. (e) Live smoke verification with REAL creds (gated/skippable): OpenRouter + local vLLM completion round-trip, and Telegram + SMTP send, recording message ids in the journal; keep these as creds-gated marked tests so default CI stays green. (f) Reconcile DEFERRED_ISSUES.md against plan/MVP3_DEFERRED_BACKLOG.md: strike every MVP2-closed item with its commit, and add a 'Punted to MVP4' section (Amazon PA-API, Micro Center, eBay Application Growth Check, paperless, full community-signal pipeline) — this was an unmet MVP2 exit criterion. (g) Record the paperless (receipt/invoice archiving) decision explicitly as optional/MVP4 per Sean's stated preference; do not build it in MVP3. (h) Verify operational hygiene: the Bitwarden temp session (/tmp/.bwsess) is already clean (confirmed 2026-06-02) — just assert no secrets linger. TDD where code changes (bcrypt verify/rehash tests, category tests); the ruff/doc/reconciliation work is mechanical but must keep the full suite green.
---
### feature-005: Frontend Feature Surfacing + Real Build Verification (design-system extension) [HIGH]
Surface every MVP2/MVP3 backend capability in the established dark-terminal/amber/monospace UI, and ACTUALLY build-verify it. Hard requirement: run `npm install && npm run build` (next build) and fix any type/build errors — MVP2's frontend was never compiled. Scope: (a) LIVE LISTINGS tab: replace the (n) placeholder with real scored listings per item (deal score badge, classification, scam flag, price/total, seller, and a SOURCE badge eBay/Shopify/PCPartPicker), reading the deals/listings endpoints. (b) HISTORY tab: verify + polish the already-built PriceTrendChart (recharts) and draw the sold-comps median/IQR band (feature-001) + vs-median delta. (c) AI ANALYSIS panel in the listing/detail view reading GET /ai/{listing_id}: deal grade + reasoning, scam signal chip, extracted specs table; hidden/empty-state when AI disabled. (d) Complete the G5 sonner toast wiring: wrap optimistic toggle/delete/interval-change handlers (item card + detail) in try/catch -> revert + error toast (toast infra already mounted). (e) Settings: digest-mode + per-channel score-threshold + mute controls UI bound to the notification settings endpoints. (f) Dashboard/observability: surface budget + key Prometheus counters in the existing stat-card/budget-bar style. ALL components reuse lib/api.ts and match the existing amber/mono components. TDD: component/interaction tests where a test runner exists (or documented manual verification) PLUS the mandatory passing next build. Depends on feature-001 (sold-comps data), feature-002 (cookie auth + middleware it must not regress), feature-003 (source badges).
**Dependencies:** feature-001, feature-002, feature-003
---
### feature-006: (Stretch) Semantic Catalog Matching via pgvector [LOW]
STRETCH / lower priority. Improve listing -> catalog-item attribution and 'similar items' with embeddings. Switch the Postgres image to pgvector/pgvector:pg17 (DEFERRED_ISSUES B5), add a vector column for catalog items and/or listing titles, embed via the configured AI provider (OpenRouter/local vLLM embeddings; gated by AI_ENABLED and degrade gracefully when off), and use cosine similarity to (1) suggest the best catalog item for an unmatched/ambiguous listing and (2) power a 'similar tracked items' affordance. Keep it fully optional and behind a flag so the core app runs without it. TDD: embedding client mock + similarity ranking on fixtures; migration test for the vector column; app-works-with-feature-disabled test. Depends on feature-003 only loosely (source-aware listings); can also stand alone.
**Dependencies:** feature-003
---
### feature-007: (Stretch) Community-Signal Ingestion — Reddit r/homelabsales + STH (NLP leads) [LOW]
STRETCH / lowest priority (could slip to MVP4 — see plan/MVP3_DEFERRED_BACKLOG.md A). A DISTINCT ingestion class from the price-poll adapters: peer-to-peer/community deal posts are unstructured free text, not structured listings. Build a 'community signal' pipeline that pulls from Reddit r/homelabsales (official OAuth API, free tier, ~60-100 req/min) and optionally ServeTheHome deals threads (XenForo), then uses the configured AI provider (OpenRouter/vLLM via the existing AIClient) to EXTRACT structured fields from prose (model, price, condition, location, sold/traded status). These feed a LEADS/ALERTS surface, NOT the scored-listing/notification pipeline (free-text, fast-moving, high signal-to-noise). Requirements: a new SourceAdapter-adjacent 'CommunitySignalSource' with its own polite rate bucket; a sold/traded detector to drop stale posts; dedup; a leads table + endpoint; gate the whole thing behind ENABLE_COMMUNITY_SIGNAL=false. Reddit API creds configurable + creds-gated tests. Surface matched leads against tracked catalog items in the UI (feature-005 patterns) under a clearly-separate 'COMMUNITY' affordance. TDD: NLP-extraction parse tests on captured fixtures (mock the LLM), sold/traded-filter test, dedup test, app-works-when-disabled test. Depends on feature-003 (source/rate-bucket patterns) and benefits from feature-006/AIClient.
**Dependencies:** feature-003
---
## Execution Batches
- Batch 1 (Parallel): feature-001, feature-002, feature-003, feature-004 (merge to target branch)
- Batch 2 (Parallel): feature-005, feature-006, feature-007
